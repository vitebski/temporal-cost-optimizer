services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    image: temporal-cost-optimizer-backend:local
    ports:
      - "8080:8080"
    volumes:
      - ./.env:/app/.env:ro
    restart: unless-stopped
